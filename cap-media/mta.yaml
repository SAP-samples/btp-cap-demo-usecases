_schema-version: '3.1'
ID: cap-media
version: 1.0.0
description: "CAP Media with MalwareScan Service"
parameters:
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npx -p @sap/cds-dk cds build --production
modules:
  - name: cap-media-srv
    type: nodejs
    path: gen/srv
    parameters:
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: cap-media-db
      - name: cap-media-destination-srv
      - name: cap-media-auth
  - name: cap-media-destinations
    type: com.sap.application.content
    requires:
      - name: cap-media-destination-srv
        parameters:
          content-target: true
      - name: cap-media-malwarescanner
        parameters:
          service-key:
            name: malwarescanner-key
    parameters:
      content:
        subaccount:
          existing_destinations_policy: update
          destinations:
            - Name: cap-media-malwarescanner-dest
              ServiceInstanceName: cap-media-malwarescanner
              ServiceKeyName: malwarescanner-key
    build-parameters:
      no-source: true
  - name: cap-media-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: cap-media-db
resources:
  - name: cap-media-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: cap-media-destination-srv
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-name: cap-media-destination-srv
      service-plan: lite
  - name: cap-media-malwarescanner
    type: org.cloudfoundry.managed-service
    parameters:
      service: malware-scanner
      service-name: cap-media-malwarescanner
      service-plan: clamav
  - name: cap-media-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: cap-media-${space}
        tenant-mode: dedicated
